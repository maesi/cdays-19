language: node_js
node_js:
  - "10"
before_install:
  - npm ci
script:
  - npm run build
deploy:
  - provider: script
    skip_cleanup: true
    script: node scripts/deploy.js $FTP_HOST $FTP_USER $FTP_PASSWORD
